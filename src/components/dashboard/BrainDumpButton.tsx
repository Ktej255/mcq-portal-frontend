/**
 * BrainDumpButton — stub. The BrainDumpModal (Geography day-based feature)
 * has been removed. This component renders nothing until a replacement
 * quick-note feature is built into the LMS.
 */
export function BrainDumpButton() {
  return null;
}
